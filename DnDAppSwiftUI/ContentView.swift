import SwiftUI

struct ContentView: View {
    var body: some View {
        CampaignRootView()
    }
}

#Preview {
    CampaignRootView()
        .frame(minWidth: 900, maxWidth: .infinity, minHeight: 600, maxHeight: .infinity)
}
    
